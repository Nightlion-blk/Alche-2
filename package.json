{
  "name": "al-che",
  "version": "1.0.0",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "cd frontend && npm install autoprefixer tailwindcss postcss --no-save && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "path": "^0.12.7"
  }
}